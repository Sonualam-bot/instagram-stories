export const STORY_DURATION_MS = 5000

// Fraction of the viewer's width treated as the "previous" tap zone (left side).
// The remainder is the "next" tap zone (right side).
export const TAP_ZONE_SPLIT = 0.4

export const STORIES_DATA_URL = '/stories.json'
